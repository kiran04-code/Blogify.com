<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <title>Home Page</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #ff5f6d,#ffc371, rgb(235, 91, 91) ); /* Matching navbar */
            height: 100vh;
        }
        .blog-container {
            background: white;
            padding: 30px;
            height: 70vh;
            border-radius: 12px;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
            width: 100%;
            max-width: 800px;
        }
        .blog-title {
            font-size: 26px;
            font-family: futura;
            font-weight: bold;
            text-align: center;
            margin-bottom: 20px;
            color: transparent;
            background: linear-gradient(135deg, #ff5f6d, #ffc371);
            -webkit-background-clip: text;
        }
        #inp, textarea {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 6px;
            font-size: 16px;
        }
        textarea {
            resize: vertical;
            min-height: 150px;
        }
        .main {
            height: 100vh;
            display: flex;
            align-items: center;
            flex-direction: column;
            justify-content: center;
        }
        #button {
            width: 100%;
            background: linear-gradient(135deg, #ff5f6d, #ffc371);
            color: white;
            font-size: 18px;
            padding: 14px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: 0.3s ease-in-out;
        }
        #button:hover {
            background-color: #218838;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .blog-container {
                padding: 25px;
                max-width: 500px;
            }
        }
        @media (max-width: 480px) {
            .blog-container {
                padding: 20px;
                max-width: 90%;
            }
            .blog-title {
                font-size: 22px;
            }
            #inp, textarea, #button {
                font-size: 14px;
                padding: 10px;
            }
        }

        .upload-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap:20px;
            margin-top: 15px;
        }

        .file-input {
            flex: 1;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .file-input input {
            padding: 10px;
            border-radius: 6px;
            border: 1px solid #ccc;
            width: 70%;
        }

        .file-input label {
            font-size: 14px;
        }
        .upload-container{
            display: flex;
            margin-bottom: 10px;
            align-items: center;
             
            }
        #button1[type="submit"] {
            padding: 12px;
            border-radius: 6px;
            background-color: #28a745;
            color: white;
            font-size: 16px;
            cursor: pointer;
            width: 30%;
            border: none;
        }

        #button1[type="submit"]:hover {
            background-color: #218838;
        }
        label{
      font-family: poppins Medium;
    
    
        }
        label p{
            font-size: 15px;
            white-space: nowrap;
        }
    </style>
</head>
<body>
    <%- include('partials/nav') %> 
    <div class="main">
        <div class="blog-container">
            <h2 class="blog-title">Create a Blog Post</h2>
            <form action="/post" method="post" enctype="multipart/form-data">
            <div class="upload-container">
                    <div class="file-input">
                        <label for="coverImage"><p>Upload Cover Image</p></label>
                        <input type="file" id="coverImage" name="converImageIUrl"  required> 
                    </div>
            </div>
            
                <input id="inp" type="text" name="title" placeholder="Enter Blog Title" required>
                <textarea name="body" placeholder="Write your content here..." required></textarea>
                <button id="button" type="submit">Post Blog</button>
            </form>
         
        </div>
    </div> 
    <%- include('partials/script') %>
    <%- include('partials/footer') %>
</body>
</html>
