<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <title>Signup</title>
</head>
<body>

    <style>
      body {
            background: linear-gradient(135deg, #ff5f6d, #ffc371);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }
        .signup-container {
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }
        .signup-title {
            font-size: 24px;
            font-weight: bold;
            background: linear-gradient(90deg, #ff758c, #ff4e50);
            -webkit-background-clip: text;
            color: transparent;
        }
        .btn-custom {
            background: linear-gradient(90deg, #ff758c, #ff4e50);
            color: white;
            border: none;
        }
        .btn-custom:hover {
            opacity: 0.9;
        }
        .social-buttons {
            display: flex;
            gap: 10px;
            margin-top: 15px;
        }
        .social-button {
            flex: 1;
            padding: 10px;
            border: none;
            border-radius: 5px;
            color: white;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
        }
        .google-btn {
            background: linear-gradient(135deg, #EA4335 20%, #FBBC05 40%, #34A853 60%, #4285F4 80%);


        }
        .facebook-btn {
            background: #4267B2;
        }
        .social-button:hover {
            opacity: 0.8;
        }
        .alert-danger{
     position: relative;
     top: 20px;
     color: red;
    border: 1px solid red;
    border-radius: 10px;
    font-size: 10px;
    font-family: poppins Medium ;
    }
        /* Responsive Styles */
        @media (max-width: 576px) {
            .signup-container {
                padding: 20px;
                width: 90%;
            }
           .signup-title{
             font-family: futura;
            font-size: 15px;
            }
        }
        
        @media (max-width: 768px) {
            body {
                padding: 10px;
            }
            .signup-container {
                max-width: 350px;
            }
        }
    </style>
<div class="signup-container">
    <h2 class="signup-title">Create an Account</h2>
     <form action="/sign" method="post">
        <div class="mb-3">
            <input type="text" class="form-control" name="fullName" placeholder="Full Name" required>
        </div>
        <div class="mb-3">
            <input type="email" class="form-control" name="email" placeholder="Email Address" required>
        </div>
        <div class="mb-3">
            <input type="password" class="form-control" name="password" placeholder="Password" required>
        </div>
        <button type="submit" class="btn btn-custom w-100">Sign Up</button>
    </form>
    <div class="mt-3 text-center">
        <p>Or sign up with</p>
        <div class="social-buttons">
            <button class="social-button google-btn"><i class="fa-brands fa-google" style="color: black;"></i> Google</button>
            <button class="social-button facebook-btn"><i class="fa-brands fa-facebook" style="color: black;"></i> facebook</button>
        </div>
    </div>
    <% if (locals.error) { %>
        <div class="alert alert-danger" role="alert">
            <%= locals.error%>
           </div>
    <% } %>
</div>
    <%- include('partials/script') %>
</body>
</html>